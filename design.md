# Get Healthy — Design System (extracted from code)

이 문서는 `index.html`에 실제로 적용되어 있는 디자인을 코드에서 역추출해 정리한 것입니다.
디자인 파일이 따로 없어서 실제 스타일 값(색상 hex, 폰트 사이즈, radius 등)을 전부 코드에서
grep/파싱해서 뽑았고, 스크린샷으로 실제 렌더링도 같이 남겼습니다.

작성 방식상 "의도된 디자인"이 아니라 "코드에 실제로 존재하는 값들"을 그대로 나열했기 때문에,
같은 역할인데 값이 다른 곳들이 꽤 나왔습니다. 그런 부분은 각 섹션 마지막
**⚠️ 모순/불일치** 항목에 정리했고, 맨 아래 [모순 종합 정리](#모순--불일치-종합-정리)에 전체
목록을 모아뒀습니다. 고칠지 말지는 검토 후 결정하시면 됩니다 — 이 문서 자체는 코드를 바꾸지
않았습니다.

---

## 1. 컬러 팔레트

앱 전역에서 쓰는 이름 있는 팔레트는 `c = {...}` 객체 하나입니다 (index.html 내 React 번들,
`c` 변수).

![팔레트](docs/design-assets/palette.png)

| 이름 | Hex | 주 용도 |
|---|---|---|
| `navy` | `#253A82` | 기본 텍스트/브랜드 컬러, 헤더 배경, primary 버튼 |
| `blue` | `#88A2FF` | 저녁(pm) 슬롯 칩, 카드 그림자 틴트, 2.5mg 테마 |
| `hotPink` | `#FF75CF` | 아침(am) 슬롯 칩 |
| `pink` | `#FFB2F7` | 5.0mg 테마, 음주 칩 |
| `lavender` | `#AB9DFF` | 필요시(prn) 슬롯 칩, 관리 화면 액센트 |
| `neon` | `#E3FC87` | 목통증 칩, 업데이트 배너, "복원" 버튼, 강조용 형광색 |
| `sky` | `#C0E0FF` | 연한 배경/보더 |
| `lightBlue` | `#B3C4FF` | 연한 배경/보더 (sky와 거의 동일 계열) |
| `midGray` | `#BBBBBB` | 슬롯 없는 항목 fallback 색 |

### 읽기 좋은 텍스트 컬러 매핑 (`ue()` / `Z3`)

파스텔 배경 위에 쓸 어두운 텍스트 색을 구하는 헬퍼 `ue = t => Z3[t] || t` 가 있고, `Z3`는
아래 10개 항목의 하드코딩된 매핑 테이블입니다.

![readable-text 매핑](docs/design-assets/palette.png)

```
#BBBBBB → #6b7280      #AB9DFF → #6B5BCF      #88A2FF → #3D5BC8
#C0E0FF → #3D5BC8      #B3C4FF → #3D5BC8      #FFB2F7 → #D6469E
#FFD6F5 → #D6469E  ⚠️  #FF75CF → #E0359E      #E3FC87 → #bce010
#D6F25E → #bce010  ⚠️
```

⚠️ **`#FFD6F5`, `#D6F25E`는 `c` 팔레트에 없는 색**입니다. 즉 `Z3` 테이블 안에만 존재하는
"고아 색상"이고, 실제로 `#D6F25E`는 다른 곳(작은 원형 인디케이터)에서 직접 하드코딩된 hex로
한 번 더 등장합니다 — 이름 없이 재사용되는 색.

---

## 2. 타이포그래피

폰트 패밀리가 **3가지**로 갈라져 있습니다:

- `'Apple SD Gothic Neo','Noto Sans KR',sans-serif` — 본문 대부분
- `'Apple SD Gothic Neo',sans-serif` — 일부 컴포넌트 (Noto Sans KR 폴백 없음)
- `-apple-system,BlinkMacSystemFont,"Apple SD Gothic Neo",sans-serif` — 업데이트
  배너 (React 밖의 순수 JS 스크립트라 완전히 다른 스택)

### 폰트 사이즈

코드에 등장하는 `fontSize` 값 전부 (총 19종, 사용 빈도순 상위):

```
11 (×78)  10 (×47)  14 (×41)  13 (×39)  12 (×38)  9 (×19)  15 (×10)
20 (×9)   10.5 (×7)  8 (×5)   16 (×4)   11.5 (×4)  30 (×3)  22 (×3)
18 (×2)   9.5 (×1)   8.5 (×1)  21 (×1)   17 (×1)
```

⚠️ 정수 기반의 일관된 스케일(예: 4의 배수)이 아니라, `.5` 단위 미세조정 값(8.5 / 9.5 / 10.5 /
11.5)이 섞여 있습니다. 대부분 특정 위치 하나에서만 쓰이는 걸로 봐서, 그때그때 "1px 더 작게"
식으로 조정한 값으로 보입니다.

---

## 3. 스페이싱 & radius

### Border-radius

코드에 등장하는 값(개수순): **12, 10, 20, 14, 8, 16, 6, 22, 4, 3, 18, 2, 9, 5** + `50%`
(원형용) — 총 14개의 서로 다른 픽셀 값.

카드형 컴포넌트만 봐도:
- 섹션 카드(`w1`/`cw`, 흰 배경 박스): `18`
- 복약 카드 행(X3 row): `12`
- G1 칩(증상/특이사항 등 알약형 배지): `20`
- 슬롯 배지(아침/저녁/필요시 미니 칩): `6`
- 필요시 그룹 박스: `12`
- 버튼: `10` / `12` / `14` (높이에 따라 제각각)

⚠️ 명확한 규칙(예: 카드=18, 배지=6, 버튼=12 같은 고정 스케일)이 안 보이고, 비슷한 성격의
요소끼리도 radius가 다릅니다.

### 카드 그림자

```
0 3px 12px {blue}12   ← 기본 섹션 카드(w1/cw)
0 3px 12px {blue}15
0 3px 12px {blue}18
0 4px 14px {navy}30
0 4px 16px {navy}30
0 4px 16px {neon}55   ← 업데이트 배너
```

blur/offset 조합이 3가지(3px+12px, 4px+14px, 4px+16px)로 갈라져 있습니다.

### Input / 버튼 보더 컬러

연회색 보더가 3가지 다른 hex로 등장: `#d1d5db`(6곳), `#e2e8f0`(5곳), `#eef0f3`(1곳) — 눈으로
구분은 거의 안 되지만 값 자체는 통일 안 돼 있습니다.

### 버튼 높이 × radius 조합

```
36px → 10   40px → 10   42px → 12   44px → 10 / 12 / 14 (셋 다 존재)
```

44px 높이 버튼 하나만 봐도 radius가 10/12/14로 3갈래입니다.

---

## 4. 아이콘 시스템

아이콘 렌더러가 **2개** 있습니다 — `A2`(복약/활동용, 24×24 grid)와 `D2`(증상용, 24×24 grid).
구조는 거의 동일(`{id → path}` 맵 + 단일 `<path fill>`)하지만 완전히 별개 컴포넌트입니다.

![아이콘 세트](docs/design-assets/icons.png)

**A2 (복약/활동)** — `pill`, `syringe`, `dining`, `drop`, `kit`, `bottle`, `drinking`,
`cardio`, `strength`, `travel`, `biztrip`. 이 중 `pill / syringe / dining / drop / kit /
bottle`은 이번에 구글 Material Symbols에서 실제 데이터를 가져와 교체한 것들(`*` 표시),
나머지(`drinking / cardio / strength / travel / biztrip`)는 예전부터 있던 손으로 그린 경로.

**D2 (증상)** — `nausea, vomiting, bodyache, heat, diarrhea_s, constipation,
muscle_pain, indigestion, fatigue, hives, cravings, headache, orthostatic,
brownDischarge, hunger, belch, other`. 전부 손으로 그린 경로, Material 아이콘 전환 전.

⚠️ **`syringe`와 `kit`이 지금 완전히 같은 경로(Material `mixture_med`)** 를 씁니다 — 사용자
요청대로 그렇게 반영한 거지만, 두 아이콘이 시각적으로 구분이 안 되는 상태라는 점은 명시해둡니다
(주사/침 항목과 비상약 항목이 필요시 목록에 같이 있으면 아이콘만으로는 구분 불가).

⚠️ `A2`는 아이콘별로 `fillRule`을 `nonzero`/`evenodd` 중 골라 쓰는 예외 리스트가 있는데
(`["dining","drop","kit","bottle","pill","syringe"]` = nonzero, 나머지 = evenodd),
`D2`는 이런 분기 없이 **항상 evenodd 고정**입니다. 두 아이콘 시스템이 구조는 같은데 규칙은
다릅니다.

---

## 5. 핵심 컴포넌트

### 섹션 카드 — `w1` / `cw`
흰 배경, `borderRadius:18`, `padding:16`, `boxShadow: 0 3px 12px {blue}12`.
`cw`는 접고 펼 수 있는 버전(`w1` + 화살표 토글). 앱 대부분의 흰 카드(증상/식욕/배변/컨디션/
복약/메모 등)가 이 컴포넌트입니다.

### 알약형 배지 — `G1`
`display:inline-flex`, `padding:"0.42em 12px 0.28em"`, `borderRadius:20`, `fontSize:12`,
`border:1px solid`. 증상/특이사항 칩 등에서 사용.

⚠️ 패딩이 `em` 단위(`0.42em 12px 0.28em`)인데, 앱의 다른 거의 모든 곳은 `px` 고정값을
씁니다. 폰트 크기가 상속으로 바뀌는 컨텍스트에 이 칩이 들어가면 다른 칩들과 세로 패딩이
미묘하게 달라질 수 있습니다.

⚠️ `G1`은 자체적으로 `Z3`의 **부분집합만** 하드코딩해서 갖고 있습니다(`#BBBBBB, #AB9DFF,
#88A2FF, #C0E0FF, #B3C4FF, #FFB2F7, #FFD6F5` — 7개, `hotPink`/`neon`/`D6F25E` 없음).
`tc`(텍스트 컬러)를 명시하지 않고 `color`에 `c.hotPink`나 `c.neon`을 넘기면 이 매핑에 없어서
`l||c.navy`로 폴백 — **텍스트 색이 배경색과 같아져 버릴 수 있는** 조합입니다(예: 배경
hotPink에 텍스트도 hotPink). 실제로 지금 이 컴포넌트에 hotPink/neon을 색으로 넘기는 호출이
있는지는 별도 확인이 필요합니다.

### 슬롯 배지 — 아침/저녁/필요시
```
am: ["아침", c.hotPink]   pm: ["저녁", c.blue]   prn: ["필요시", c.lavender]
```
이 3줄짜리 맵이 **코드에 3번 독립적으로 정의**되어 있습니다 (요약 화면, 편집 폼, 관리 모달
각각). 공유 상수가 아니라서, 색을 바꿀 때마다 3곳을 다 고쳐야 합니다 (이번 세션에서 실제로
"반대로 됐다"는 피드백을 받고 3곳을 한 번에 replace한 이력이 있습니다).

---

## 6. 실제 화면 스크린샷

### 홈 / 일일 기록 (읽기 전용)
![홈](docs/design-assets/screen-home.png)

### 복약 요약 (카테고리별 한 줄 + 아이콘/텍스트)
![복약 요약](docs/design-assets/screen-meds-summary.png)

### 기록 편집 폼 — 복약 섹션 (아침→저녁→필요시 순서, 용량 드롭다운)
![복약 편집](docs/design-assets/screen-meds-edit.png)

### 기록 편집 폼 — 필요시 그룹 박스
![필요시](docs/design-assets/screen-meds-prn.png)

### 복약 관리 모달
![복약 관리](docs/design-assets/screen-med-management.png)

### 전체기록 탭 (누적 통계 / 백업·복원 / 목표 / 차트)
![전체기록](docs/design-assets/screen-alltime.png)

### 분석 탭 (패턴 분석 — 헤더가 회색 테마로 바뀜)
![분석](docs/design-assets/screen-analysis.png)

⚠️ 분석/전체기록 탭에서는 헤더 배경이 평소의 페리윙클 블루/핑크 톤이 아니라 **무채색 회색
(`#C8C8D0`)** 테마로 바뀝니다 (`ja(null)` 폴백). 의도된 것인지, 그냥 dose 테마가 없을 때의
기본값이 우연히 이렇게 보이는 건지는 코드만 봐서는 불명확합니다.

---

## 모순 / 불일치 종합 정리

내일 검토하실 수 있게 위에서 나온 것들을 한 곳에 모았습니다.

1. **슬롯 색상 맵이 3곳에 중복 정의됨** (`am/pm/prn` 배지) — 공유 상수화 안 돼 있어서 색
   바꿀 때마다 여러 곳을 고쳐야 함.
2. **`Z3`(읽기용 텍스트 컬러 매핑)에 `c` 팔레트에 없는 색 2개**(`#FFD6F5`, `#D6F25E`)가
   들어있음 — 이름 없이 하드코딩된 색이 팔레트 밖에 따로 존재.
3. **`G1` 칩이 `Z3`의 부분집합(7/10)만 자체 하드코딩**하고 있어서, `hotPink`/`neon`을
   `color`로 넘기면 텍스트 색이 배경색과 같아져 버릴 수 있음(폴백이 `l`, 즉 배경색 자체).
4. **폰트 패밀리가 3가지 다른 스택**으로 갈라져 있음 (React 앱 내부에서도 2종류 + 업데이트
   배너용 순수 JS가 또 다른 스택).
5. **폰트 사이즈가 19종**이고 `.5` 단위 미세조정 값이 다수 — 일관된 타입 스케일 없음.
6. **Border-radius가 14종**의 서로 다른 값 — 카드/배지/버튼 각각 성격이 비슷한데도 radius
   불일치.
7. **버튼 높이×radius 조합이 들쭉날쭉** — 44px 버튼만 해도 radius 10/12/14 세 가지.
8. **Input 보더 컬러가 3가지 유사 회색**(`#d1d5db` / `#e2e8f0` / `#eef0f3`)으로 흩어짐.
9. **카드 그림자가 3가지 blur/offset 조합**으로 나뉨.
10. **`G1` 칩만 패딩이 `em` 단위**, 나머지는 전부 `px` — 폰트 크기 상속 상황에서 다른 칩과
    세로 정렬이 달라질 수 있음.
11. **아이콘 렌더러가 `A2`/`D2` 두 개로 나뉘어 있고 규칙이 다름** — `A2`만 아이콘별
    `fillRule` 예외 처리가 있고 `D2`는 항상 `evenodd` 고정.
12. **`syringe`와 `kit` 아이콘이 완전히 동일한 경로**(Material `mixture_med`)를 사용 —
    필요시 목록에서 두 종류가 아이콘만으로는 구분 안 됨. (이번 세션 요청대로 반영된 것이라
    의도된 상태일 수 있음, 확인 필요.)
13. **분석/전체기록 탭 헤더가 무채색 회색 테마로 바뀜** — 나머지 탭의 페리윙클 블루/핑크
    테마와 다른 계열. 의도인지 기본값 누락인지 불명확.
14. **D2(증상) 아이콘 17종 전부 Material 미전환 상태** — CLAUDE.md에 새로 추가한 "새 아이콘은
    구글 Material에서 찾아 쓰기" 규칙이 아직 소급 적용 안 된 레거시 세트. (버그는 아니고,
    새 규칙 도입 전에 만들어진 것들이라 별도 마이그레이션이 필요하면 참고.)
